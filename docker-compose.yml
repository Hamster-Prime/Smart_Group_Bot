services:
  bot:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./config.toml:/app/config.toml:ro
    ports:
      # 入群验证页（Cloudflare Turnstile），对应 JOIN_VERIFICATION_LISTEN_PORT
      - "8480:8480"
    restart: unless-stopped